<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#4099FF</item>
        <!-- Details color -->
        <item name="colorAccent">#4099FF</item>
        <!--<item name="android:clipChildren">false</item>-->
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
        <item name="android:statusBarColor">@color/blueHundred</item>
    </style>

    <style name="GoldAccent" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">#EBBA2D</item>
    </style>

</resources>
